pcvp_manager:
  requires:
    - pcvp_manager/3.1.0@semsa/unstable  # needs to be exact since 2.10 fails
  node: localhost

yolov7_pcvp:
  requires:
    - yolov7_pcvp/[^2.3.1]@semsa/unstable
  options:
  node: localhost

dense_pose_pcvp:
  requires:
    - dense_pose_pcvp/2.4.5@semsa/unstable
  options:
  node: localhost

m3t_refiner_pcvp:
  requires:
    - m3t_refiner_cpp_pcvp/0.3.0@semsa/unstable
  node: localhost

flask_server_pcvp:
  requires:
    - flask_pcvp/0.10.0@semsa/unstable
  node: localhost


#guide_dog_server_pcvp:
#  requires:
#    - pcvp_client_py/3.2.0@semsa/unstable
#    - opencv-python/4.9.0.80@pypi/stable
#  node: localhost
