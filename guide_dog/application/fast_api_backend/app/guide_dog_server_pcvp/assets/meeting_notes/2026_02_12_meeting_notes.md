# meeting 12.02.2026

### Max, Leo, Felix, Anne

### Status

MVP is working!! - we are amazing :D


### How far do we want to come in the next weeks?

Requirements for:

- RIG (11th March)
  -  MVP currently running with external access over ssh port-forwarding
  -  use live camera

- Usage within institute (End of March)
  - one system uses it with one access key. with pre-defined objects + pipeline
  - pipeline health status (sanity, up / down requests) - info that pipeline needs restart, json request adaption for "put camera"
  - multiple users with multiple access keys, with pre-defined objects + pipeline, associate user with pipeline either other session id or access key
  -

- Meeting with Admins (Beginning of April)
  - User authentication is more than a hard-coded if/else

- Future features
  - add pipeline modules on-demand
  - adapt which objects to detect, load models "on-the-fly" or as part of a setup request
  - extend to new modules? e.g for Nemo
